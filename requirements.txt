# Food Delivery System — Integrated Requirements
# Install: pip install -r requirements.txt

fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
pydantic[email]==2.7.1
pydantic-settings==2.2.1
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1

# Optional: install only if using real Stripe
# stripe==15.1.0

# Optional: install only if using PostgreSQL instead of SQLite
# psycopg2-binary==2.9.9
# asyncpg==0.29.0

# Optional: install only if using Alembic migrations
# alembic==1.13.1
